from django.apps import AppConfig


class AdminpanelConfig(AppConfig):
    name = 'adminpanel'

# This tells Django:
# "There is an app called adminpanel."
# Django uses this file to recognize the app.
# Nothing complicated here.